import { MergeObjectsPipe } from './merge-objects.pipe';

describe('MergeObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new MergeObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
